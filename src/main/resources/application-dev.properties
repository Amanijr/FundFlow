# Local evaluation profile — dummy CrossLife org and demo users.
# Activated by `./mvnw spring-boot:run` (spring-boot-maven-plugin) or docker-compose.dev.yml.
app.dev.seed-enabled=true
server.error.include-message=always
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
