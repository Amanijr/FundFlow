# Official Postgres, tagged as FundFlow DB so `docker compose build` produces all three images.
FROM postgres:16-alpine

LABEL org.opencontainers.image.title="FundFlow DB" \
      org.opencontainers.image.description="PostgreSQL 16 for FundFlow ERP"
